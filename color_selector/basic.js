"use strict";

function cssToRGB(rgb) {
  let r = rgb.substring(rgb.indexOf("(") + 1, rgb.indexOf(","));
  let g = rgb.substring(rbg.indexOf(","), rbg.lastIndexOf(","));
  let b;
  console.log(r);
}

cssToRGB("rgb(192, 13, 1)");
